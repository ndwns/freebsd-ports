# $Id$
#

    SUBDIR += bcc
    SUBDIR += bison
    SUBDIR += dmake
    SUBDIR += gmake
    SUBDIR += libdlmalloc
    BROKEN += libmalloc
    SUBDIR += m4
    SUBDIR += mprof
    SUBDIR += noweb
    SUBDIR += xxgdb

.include <bsd.port.subdir.mk>
