# $Id$
#

    SUBDIR += agrep
    SUBDIR += glimpse
    SUBDIR += isearch
    SUBDIR += ispell
    SUBDIR += mgdiff
    SUBDIR += pilot_makedoc
    SUBDIR += rman
    SUBDIR += sp
    SUBDIR += unroff

.include <bsd.port.subdir.mk>
