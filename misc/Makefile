# $Id$
#

    SUBDIR += amanda
    SUBDIR += beav
    SUBDIR += chord
    SUBDIR += colorls
    SUBDIR += git
    SUBDIR += ical
    SUBDIR += ispell
    SUBDIR += kp
    SUBDIR += less
    SUBDIR += mc
RESTRICTED += mmv               # restrictive copyright
    SUBDIR += mshell
    SUBDIR += recode
    SUBDIR += rman
    SUBDIR += screen
    SUBDIR += tkinfo
    SUBDIR += tkman

.include <bsd.port.subdir.mk>
