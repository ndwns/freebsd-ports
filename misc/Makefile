# $Id$
#

    SUBDIR += amanda
    SUBDIR += beav
    SUBDIR += chord
    SUBDIR += colorls
    SUBDIR += git
    SUBDIR += ispell
    SUBDIR += kp
    SUBDIR += less
    SUBDIR += mc
RESTRICTED += mmv               # restrictive copyright
    SUBDIR += mshell
    SUBDIR += rman
    SUBDIR += screen

.include <bsd.port.subdir.mk>
