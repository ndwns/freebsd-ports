# $Id$
#
# RESTRICTED: maxima (license required)
#

   SUBDIR += blas
   SUBDIR += calc
   SUBDIR += eispack
   SUBDIR += fudgit
   SUBDIR += gmp
   SUBDIR += gnuplot
   SUBDIR += hexcalc
   SUBDIR += linpack
   SUBDIR += octave
   SUBDIR += oleo
   SUBDIR += xgraph
   SUBDIR += xspread

.include <bsd.port.subdir.mk>
