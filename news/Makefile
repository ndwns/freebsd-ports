# $Id$
#

    SUBDIR += cnews
    SUBDIR += inn
    SUBDIR += knews
    SUBDIR += nn
    SUBDIR += nn-current
    SUBDIR += nntp
    SUBDIR += rkive
    SUBDIR += tin
    SUBDIR += trn

.include <bsd.port.subdir.mk>
